apiVersion: v1
kind: Service
metadata:
  name: devops-service
  labels:
    app: devops-api
spec:
  type: LoadBalancer
  selector:
    app: devops-api
  ports:
  - port: 80
    targetPort: 3000
    name: http
